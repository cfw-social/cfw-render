{
  "name": "cfw-render",
  "private": true,
  "description": "Decoupled render fleet for CFW Social — bash drainer, no JS runtime.",
  "scripts": {
    "lint": "scripts/lint.sh",
    "test": "test/run-tests.sh"
  }
}
